"""
openedx_keys.legacy_api — legacy and deprecated key classes.

These classes are either already deprecated or not promoted to the clean API.
New code should not use classes from this module. Existing code can import
from here or from the opaque_keys.edx.* compat shims.
"""
# Populated in impl step 11.
